"use client";
/* eslint-disable react-hooks/set-state-in-effect -- async loader callbacks update state only after awaited fetch responses. */
import Link from "next/link";
import {FormEvent,useCallback,useEffect,useState} from "react";
import styles from "../admin.module.css";
type Meeting={id:string;title:string;joinCode:string;status:string;hostName:string;participantCount:number;recordingCount:number;durationSeconds:number;createdAt:string};type Detail={meeting:Meeting;participantBreakdown:{role:string;status:string;count:number;averageDurationSeconds:number}[]};
export default function AdminMeetings(){const [meetings,setMeetings]=useState<Meeting[]>([]),[status,setStatus]=useState(""),[search,setSearch]=useState(""),[error,setError]=useState(""),[detail,setDetail]=useState<Detail|null>(null);const load=useCallback(async()=>{const query=new URLSearchParams({limit:"100"});if(status)query.set("status",status);if(search)query.set("search",search);const response=await fetch(`/api/v1/admin/meetings?${query}`),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data?.error?.message??"Could not load meetings");setMeetings(data.meetings)},[status,search]);useEffect(()=>{void load().catch(reason=>setError(reason instanceof Error?reason.message:"Could not load meetings"))},[load]);const submit=(event:FormEvent)=>{event.preventDefault();void load()};const inspect=async(id:string)=>{const response=await fetch(`/api/v1/admin/meetings/${id}`),data=await response.json().catch(()=>({}));if(!response.ok){setError(data?.error?.message??"Could not load analytics");return}setDetail(data)};return <main className={styles.page}><header><div><p>XPACE ADMIN · MEETINGS</p><h1>Meetings & analytics</h1><span>Tenant-wide meeting activity and participation insights.</span></div><nav><Link href="/admin">Dashboard</Link><Link href="/admin/users">Users</Link><Link href="/admin/groups">Groups</Link></nav></header>{error&&<p style={{padding:12,border:"1px solid #6c3438",borderRadius:8,color:"#ef9ba2"}}>{error}</p>}<form onSubmit={submit} className={styles.health}><input aria-label="Search meetings" value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search title, code, or host" style={{flex:1,padding:9,background:"#090c0a",color:"#eef2ee",border:"1px solid #303a31",borderRadius:7}}/><select aria-label="Filter status" value={status} onChange={event=>setStatus(event.target.value)} style={{padding:9,background:"#090c0a",color:"#eef2ee",border:"1px solid #303a31",borderRadius:7}}><option value="">All statuses</option>{["SCHEDULED","WAITING","ACTIVE","ENDED","CANCELLED"].map(value=><option key={value}>{value}</option>)}</select><button style={{padding:9,border:0,borderRadius:7,background:"#a3e635",color:"#142006"}}>Apply</button></form><section className={styles.panel} style={{marginTop:16}}><div className={styles.panelHead}><div><h2>Meeting list</h2><p>{meetings.length} results</p></div></div><div className={styles.table}><div className={styles.tableHead}><span>Meeting</span><span>Host</span><span>Participants</span><span>Status</span><span>Duration</span></div>{meetings.map(meeting=><button className={styles.row} key={meeting.id} onClick={()=>inspect(meeting.id)} style={{width:"100%",background:"none",color:"inherit",borderLeft:0,borderRight:0,borderBottom:0,textAlign:"left"}}><strong>{meeting.title}<small style={{display:"block",color:"#697269"}}>{meeting.joinCode} · {meeting.recordingCount} recordings</small></strong><span>{meeting.hostName}</span><span>{meeting.participantCount}</span><span className={styles[meeting.status.toLowerCase()]??""}>{meeting.status}</span><span>{formatDuration(meeting.durationSeconds)}</span></button>)}</div></section>{detail&&<div onClick={event=>{if(event.target===event.currentTarget)setDetail(null)}} style={{position:"fixed",inset:0,background:"#000b",display:"grid",placeItems:"center",padding:20,zIndex:50}}><article className={styles.panel} style={{width:"min(620px,100%)",maxHeight:"85vh",overflow:"auto"}}><div className={styles.panelHead}><div><h2>{detail.meeting.title}</h2><p>{detail.meeting.joinCode} · {formatDuration(detail.meeting.durationSeconds)}</p></div><button onClick={()=>setDetail(null)}>×</button></div><dl className={styles.usage}><div><dt>Host</dt><dd>{detail.meeting.hostName}</dd></div><div><dt>Participants</dt><dd>{detail.meeting.participantCount}</dd></div><div><dt>Recordings</dt><dd>{detail.meeting.recordingCount}</dd></div></dl><h2 style={{fontSize:14}}>Participant breakdown</h2>{detail.participantBreakdown.map((item,index)=><p key={`${item.role}-${item.status}-${index}`} style={{display:"flex",justifyContent:"space-between",color:"#8e9890",fontSize:12}}><span>{item.role.replace("_"," ")} · {item.status}</span><b>{item.count} · avg {formatDuration(item.averageDurationSeconds)}</b></p>)}</article></div>}</main>}
function formatDuration(seconds:number){const hours=Math.floor(seconds/3600),minutes=Math.floor(seconds%3600/60);return hours?`${hours}h ${minutes}m`:`${minutes}m`}
